# Roadmap (not current features)

Possible later work, none of which should be described as already shipped:

- Cross-encoder rerank
- Reproducible latency harness
- Dedicated GitHub verification for FITMAN / agent-system / LiveKit / vision
- Confirm NSU graduation vs undergraduate status
- Confirm FITMAN demo availability independently
- Optional BM25 index if a sparse backend is actually added
